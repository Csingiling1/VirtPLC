i dont want placholder implementations, nor STUBS or hardcoded values. implement everything! the backpressure DataSource uses hardcoded values not from timesaleDB or the values got from the simulator via OPC-UA. the backend doestn connect to the simulator docker which sends the data via OPC-UA. And also another things is that i want the codebase to work on any amount and any type of signal with any amount of devices, factory sectors and manufacturers