it generated this:
[artifact type="react" title="Temperature Trend Chart"]{
  "charts": [
    {
      "type": "line",
      "title": "Temperature Trend",
      "query": "SELECT device_id, timestamp, value FROM plc_data WHERE device_id LIKE '%temperature%' ORDER BY timestamp DESC LIMIT 100",
      "x_field": "timestamp",
      "y_field": "value"
    }
  ]
}
</artifact>

for this query:
Create a single comprehensive line chart showing temperature data from all available PLC devices and sensors in the factory over the last 24 hours. Include data from every device that has temperature readings. Make sure the chart has a clear legend below the chart showing which color represents each device/sensor.

but i want to write me some descriptive text and to a second tab appear and use "stream effect" to generate the code and then run it like claude does on the website!